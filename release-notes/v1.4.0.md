아마란스 그룹웨어(`gw.goorm.io`)에 들어가지 않고, 크롬 툴바에서 이번 달 근무시간과 오늘 퇴근 가능 시각을 확인하는 비공식 확장입니다.

## v1.4.0 에서 달라진 것

**오늘 근무 포함 / 제외 토글.** 오늘 탭에서 스위치 하나로, 이번 달 누적·부족·진도를 오늘 일한 시간을 넣고 볼지 빼고 볼지 고를 수 있습니다. 선택은 저장됩니다.

**하루 권장 평균.** 이번 달 부족분을 남은 근무일로 나눈 값을 보여줍니다. 하루에 이만큼씩 채우면 이번 달 소정에 맞습니다.

**이름이 자동으로 잡힙니다.** 팀근태의 내 이름과 공유 코드에 들어가는 이름을, 이제 그룹웨어에서 자동으로 읽어 옵니다. 못 읽었을 때는 조직도에서 채워 넣습니다. 공유 코드에 이름이 안 들어가는 상황이면 경고로 알려 줍니다.

**설정 화면 정리.** 버전을 맨 위로, 내 사번을 바로 볼 수 있게 했고, 자주 쓰지 않는 항목은 걷어 냈습니다.

## 설치

아래 Assets 에서 `amaranth-worktime-extension.zip` 을 받아 압축을 풉니다. 압축 푼 폴더를 먼저 최종 위치로 옮기세요 — 크롬은 이 확장을 폴더 경로로 기억하기 때문에, 로드한 뒤에 옮기거나 이름을 바꾸면 확장이 깨집니다. 바탕화면과 문서 폴더는 macOS iCloud 동기화 대상이라 파일이 클라우드로 밀려나면 확장이 멈추므로, `~/chrome-extensions/` 처럼 동기화되지 않는 곳을 권합니다.

폴더를 옮긴 다음 크롬에서 `chrome://extensions` 를 열고 오른쪽 위 **개발자 모드**를 켠 뒤, **압축해제된 확장 프로그램을 로드합니다**로 그 폴더를 선택합니다. 마지막으로 그룹웨어에 한 번 접속하면 사번과 이름을 읽어옵니다.

**이미 쓰고 있다면** 설정의 **업데이트** 버튼으로 바로 받을 수 있습니다.

## 기능

* 오늘 퇴근 가능 시각과 남은 시간 — 하루 소정 근로시간 기준, 점심시간 반영
* 이번 달 초과·부족 누적, 하루 권장 평균, 남은 근무일
* **오늘 근무 포함/제외 토글**
* 근태 달력 — 근무시간 농도, 퇴근 미등록일, 날짜별 상세
* 연차 현황 — 일반 연차·보상 휴가·대체 휴가 구분
* 휴가 달력 — 팀 전체 휴가, 부서·내 그룹 필터
* 팀근태 — 우리 팀 오늘 출근 현황, 공유 코드
* 그룹웨어 알림, 확장 안에서 업데이트

## 알아둘 점

* 조회 전용입니다. 출퇴근 등록 같은 쓰기 동작은 하지 않습니다
* 각자 브라우저의 그룹웨어 로그인 세션으로만 동작하며, 모든 데이터는 각자 브라우저에만 저장됩니다
* 그룹웨어가 공개한 API 가 아니라 웹 화면이 쓰는 요청을 흉내 냅니다. 그룹웨어가 바뀌면 동작이 멈출 수 있습니다

**Full Changelog**: https://github.com/egg-silver/amaranth-worktime-extension/compare/v1.3.0...v1.4.0
